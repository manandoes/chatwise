# Reusable interface pieces

- `ui/`          The base building blocks (buttons, inputs, cards) from shadcn/ui
- `dashboard/`   Pieces used only inside the dashboard (sidebar, top bar, stat cards)
- `onboarding/`  The wizard slides
- `marketing/`   Sections of the public website (hero, feature grid, pricing table)

All styling follows docs/Design.md.
